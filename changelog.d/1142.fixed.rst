Fix the deprecation warning for an unset `asyncio_default_fixture_loop_scope` not being reported. The warning is now issued via `Config.issue_config_time_warning`, so it correctly appears in the warnings summary and respects `-W`/`filterwarnings`.
